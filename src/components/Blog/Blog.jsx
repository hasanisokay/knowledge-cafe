import React from 'react';

const Blog = () => {
    return (
        <div>
            <p>Props vs state
How does useState work?
Purpose of useEffect other than fetching data.
How Does React work?</p>
        </div>
    );
};

export default Blog;